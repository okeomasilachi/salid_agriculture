import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["placehold.co", "www.bing.com", "salidagriculture.com", "th.bing.com", "images.unsplash.com"],
    // domains: ["https://placehold.co/600x400"],
  }
  /* config options here */
};

export default nextConfig;
